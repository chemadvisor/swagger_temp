 Link:
    type: object
    required:
      - allow
      - content_type
      - href
      - rel
    properties:
      rel:
        type: string
      allow:
        type: string
        description: The HTTP methods allowed on the href
      href:
        type: string
      content_type:
        type: string
    xml:
      name: link